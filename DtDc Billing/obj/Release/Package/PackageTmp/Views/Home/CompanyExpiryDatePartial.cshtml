@model DtDc_Billing.CustomModel.dashboardDataModel
<style>

    .blink {
        animation: blink-animation 1s steps(5, start) infinite;
        -webkit-animation: blink-animation 1s steps(5, start) infinite;
    }

    @@keyframes blink-animation {
        to {
            visibility: hidden;
        }
    }

    @@-webkit-keyframes blink-animation {
        to {
            visibility: hidden;
        }
    }

    .urlpath {
        color: black;
    }

    .products-list .product-info {
        margin-left: 0;
    }

    .products-list .product-title {
        font-weight: 600;
        font-size: 14px;
    }

    .product-info a:hover {
        color: #3c8dbc;
    }
</style>

@if (@ViewBag.After1Year >= @ViewBag.NowDate)
{
<div class="col-md-12">

    @*<marquee width="100%" direction="right" height="50px">
            Total Company Expiry Count is - @ViewBag.ExpiryCompCount
            <a href="@Url.Action("PfReportDaily", "Reports")" class="small-box-footer">More info <i class="fa fa-arrow-circle-right"></i></a>
        </marquee>*@
    <div class="box box-primary">
        <div class="box-header with-border">

            <strong style="font-size: 18px;"><i class="fa fa-bell-o" aria-hidden="true"></i><span style="color: #bc3f30; ">Updates</span></strong>
            <marquee direction="right">
                <span class="badge bg-blue blink"> Your Subscription Expires in @ViewBag.ExpiryCompCount Days</span>
                @*<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#makePaymentModel">
             Renew Now
        </button>*@
                <a href="@Url.Action("RenewalPanel", "Home")" class="small-box-footer">Renew Now <i class="fa fa-arrow-circle-right"></i></a>

            </marquee>

        </div>

    </div>


    <!--<div class="modal fade" data-email="email" id="makePaymentModel" tabindex="-1" role="dialog" aria-labelledby="makePaymentModel" aria-hidden="true" onclick="fun(this)">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Registration successful! Make payment for enjoy service</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body" id="makePayment">

                            
                <div class="modal-footer">-->
                    @*<button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                        <button type="button" class="btn btn-primary">Save changes</button>*@
                <!--</div>
            </div>
        </div>
    </div>-->
</div>

    

}
